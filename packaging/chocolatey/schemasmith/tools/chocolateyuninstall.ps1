$ErrorActionPreference = 'Stop'

# Chocolatey handles package directory removal automatically. No custom
# cleanup needed — there is no installer state outside the package
# directory and no machine-wide registration to undo.
