{
  "Name": "Main",
  "DatabaseIdentificationScript": "SELECT name FROM sys.databases WHERE name = 'learn'",
  "ScriptFolders": [
    { "FolderPath": "After Scripts", "QuenchSlot": "After" },
    { "FolderPath": "TestData", "QuenchSlot": "After", "ShouldApplyExpression": "SELECT CASE WHEN '{{Environment}}' = 'Development' THEN 1 ELSE 0 END" }
  ]
}
