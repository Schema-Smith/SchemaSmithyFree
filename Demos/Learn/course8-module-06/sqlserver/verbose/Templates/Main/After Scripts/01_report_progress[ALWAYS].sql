-- VerboseLogging demo: a user PRINT (severity 0). Suppressed by default on SQL Server;
-- surfaced when VerboseLogging is on.
PRINT 'CourseEightModuleSix VerboseLogging demo line from a user script.';
