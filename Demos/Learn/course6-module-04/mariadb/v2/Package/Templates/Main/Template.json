{
  "Name": "Main",
  "DatabaseIdentificationScript": "SELECT SCHEMA_NAME FROM information_schema.schemata WHERE SCHEMA_NAME LIKE 'shop_tenant_%'",
  "BaselineValidationScript": "SELECT COALESCE((SELECT MAX(version) FROM schema_version WHERE product = 'Shop'), 0) <= 2",
  "VersionStampScript": "INSERT INTO schema_version (product, version) VALUES ('Shop', 2) ON DUPLICATE KEY UPDATE version = VALUES(version)",
  "ScriptTokens": {}
}
