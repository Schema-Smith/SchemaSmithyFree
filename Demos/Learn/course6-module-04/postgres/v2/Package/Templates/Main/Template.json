{
  "Name": "Main",
  "DatabaseIdentificationScript": "SELECT datname FROM pg_database WHERE datname LIKE 'shop_tenant_%'",
  "BaselineValidationScript": "SELECT COALESCE((SELECT MAX(version) FROM public.schema_version WHERE product = 'Shop'), 0) <= 2",
  "VersionStampScript": "INSERT INTO public.schema_version (product, version) VALUES ('Shop', 2) ON CONFLICT (product) DO UPDATE SET version = EXCLUDED.version",
  "ScriptTokens": {}
}
