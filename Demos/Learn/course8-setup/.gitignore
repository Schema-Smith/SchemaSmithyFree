# Learner-generated run output from the Step 2 tour. Resolved-SQL artifacts are
# already ignored repo-wide (root .gitignore: "SchemaQuench - *.sql"); logs and
# checkpoints are per-run and transient (logs carry timestamps; a checkpoint only
# exists mid-failure), so keep them out of the lab too.
*/logs/
*/checkpoints/
