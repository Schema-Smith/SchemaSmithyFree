{
  "Name": "mv_active_customer_count",
  "Definition": "SELECT customer_id, COUNT(*) AS activity_count FROM \"{{SchemaName}}\".activities WHERE activity_time >= now() - INTERVAL '30 days' GROUP BY customer_id",
  "AccessMethod": "heap",
  "Indexes": [
    {
      "Name": "ix_mv_active_customer_count_customer_id",
      "Unique": true,
      "IndexColumns": "customer_id",
      "AccessMethod": "btree"
    }
  ]
}
