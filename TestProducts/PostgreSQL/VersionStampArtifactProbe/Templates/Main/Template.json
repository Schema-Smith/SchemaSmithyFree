{
    "Name": "Main",
    "DatabaseIdentificationScript": "SELECT datname FROM pg_database WHERE datistemplate = false AND datname = '{{MainDB}}'",
    "VersionStampScript": "-- version_stamp_artifact_probe_marker AdminPassword={{AdminPassword}} Region={{Region}}\nDO $$\nBEGIN\n  RAISE EXCEPTION 'version_stamp_artifact_probe_error';\nEND $$ LANGUAGE plpgsql;",
    "ScriptFolders": []
}
